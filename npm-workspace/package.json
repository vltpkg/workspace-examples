{
  "name": "npm-workspace-example",
  "version": "0.0.1",
  "description": "Example npm monorepo for publishing packages",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces"
  }
}
